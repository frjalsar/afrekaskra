providers = ["...", "python"]

[phases.setup]
nixPkgs = ['...', 'nginx']

[phases.build]
cmd = ['npm run build', 'pip install -r requirements.txt', 'cp config/nginx.conf.erb /etc/nginx/']

[start]
cmd = 'start_railway.sh'